SESSION SUMMARY — June 2026 (session 5)
Cooper Teaches Tennis | CooperTeachesTennis/CTTAcademy
================================================================================

WHAT WE BUILT
--------------

1. DELETE PLAYER
   Coaches can now permanently delete a player profile and all associated data.

   How it works:
   - Red "Delete" button appears in the Player Info header row on player.html
     (alongside Edit and Mark Inactive), visible only in view mode
   - Clicking Delete shows a confirm dialog: "Are you sure? Deleting a player
     profile is permanent and irreversible."
   - On confirm: calls DELETE /api/player/:id (coach-only endpoint)
   - Worker cleans up all KV keys for that player:
       player:{id}, lttdp:{id}, sessions:list:{id}, every session:{sessionId},
       removes id from email-index:{email} (deletes key if array becomes empty),
       removes id from email-index:{parentEmail} if different,
       removes id from players:all
   - Redirects to dashboard.html on success
   - Inline error shown if the request fails

   Design note: "Mark Inactive" remains the recommended path for players who
   pause lessons — it preserves all history. Delete is for test profiles or
   data entry mistakes.

   Files changed: worker/index.js (handlePlayerDelete + route),
                  js/player.js (button + confirm + apiDelete call),
                  js/api.js (added apiDelete function)

2. RESOURCES PAGE
   New tab in the site nav for Cooper to share resources with players: discount
   codes, Google Sheets links, his book, or anything else useful between sessions.

   Public view:
   - Anyone can visit resources.html without logging in
   - Shows two sections: "Discount Codes" (formatted text) and "Player Resources"
     (clickable links)
   - Shows a "coming soon" empty state until Cooper adds content

   Coach edit mode:
   - When Cooper is signed in, an "Edit" button appears at the top right
   - Edit mode: textarea for discount codes, dynamic list of label+URL pairs
     with "Add link" and "Remove" buttons; matches the LTTDP inline edit pattern
   - Save writes to KV as `content:resources`; changes are live immediately

   Backend:
   - GET /api/resources — public, returns content:resources from KV or empty default
   - PUT /api/resources — coach-only, validates and saves updated content

   Files created: resources.html, js/resources.js
   Files changed: worker/index.js (handleResourcesGet, handleResourcesPut, routes)

3. NAV COACH SWAP FIX
   Bug: when a coach navigated to public tabs (Home, Store, About Me, etc.) the
   tab nav showed "Player Profile" instead of "Your Players", and the top-right
   showed "Sign In" instead of "Sign out".

   Root cause: public pages hardcode the player-facing nav; there was no mechanism
   to detect a signed-in coach and adjust the labels.

   Fix: added swapNavForCoach() to auth.js, fired on DOMContentLoaded on every
   page. If a coach token exists in sessionStorage:
   - Finds the "Player Profile" tab (href="index.html") and swaps it to
     "Your Players" → dashboard.html
   - Finds the "Sign In" top-nav link and replaces it with "Sign out" behavior
     (calls CTTAuth.logout())
   No network call needed — purely reads sessionStorage.

   Files changed: js/auth.js

4. RESOURCES TAB ADDED TO ALL PAGES
   The Resources tab appears in the tab nav across every page of the site,
   positioned between Education and Link Tree.

   Tab order: Home | Player Profile / Your Players | Store | Education |
              Resources | Link Tree | About Me

   Files changed: dashboard.html, player.html, session.html, home.html,
                  store.html, education.html, about.html, linktree.html,
                  profile.html


5. RESOURCES PAGE — DESCRIPTION FIELD + BOLD FORMATTING
   Two additions to the Resources page after initial build:

   Description field:
   - Each Player Resources link now has a third field: Description (optional)
   - Stored as `description` on each link object in `content:resources` KV
   - Displayed below the link label in view mode in smaller muted text
   - Supports bold formatting same as other fields
   - Worker updated to persist description through handleResourcesPut

   Bold formatting:
   - Implemented as lightweight **markdown** — select text, click B, it wraps
     in ** markers (e.g. **bold text**)
   - B button above the Discount Codes textarea
   - B button next to each link's Description field
   - renderMarkdown() renders ** markers as <strong> tags server-side for
     the Discount Codes block (also handles newlines → paragraphs)
   - renderInline() handles ** markers in link labels and descriptions
   - No libraries — pure vanilla JS selection API (selectionStart/End)
   - XSS-safe: escHtml() always runs before ** substitution

   Files changed: js/resources.js (rewritten), worker/index.js (description field)


DESIGN DECISIONS MADE THIS SESSION
-------------------------------------
- Hard delete exists alongside Mark Inactive. Confirmed dialog wording is
  intentionally short and direct: "Are you sure? Deleting a player profile is
  permanent and irreversible." — no data details in the message.
- Resources page is fully public (no auth to view), matching the pattern of
  Home, About Me, Education. Only write operations require coach auth.
- Resource links section (Google Sheets, book, etc.) uses a single unified
  label+URL list rather than separate sections per category — simpler for
  Cooper to manage and easy to extend with more links later.
- Nav coach swap runs synchronously from sessionStorage (no network round-trip)
  since it's purely a UI label change; an expired token still shows "Your Players"
  but clicking it redirects to index.html via dashboard.html's auth guard.


OPEN / DEFERRED
-----------------
1. Populate Resources page with real content — Cooper to add:
   - Discount codes when he has them
   - Google Sheets URLs he maintains for players
   - Singles Strategy book retail link
2. Parent/guardian account model redesign — still deferred
3. Confirm Cooper's real social media URLs → update linktree.html
4. Update About Me content in worker/index.js → handleGuestInfo() with
   Cooper's real bio
5. Custom domain setup (cooperteachestennis.com) via Cloudflare when ready
6. GitHub Pages on CooperTeachesTennis/CTTAcademy — requires Cooper to enable
   in repo Settings (org admin access — Mitch can't do it)
7. Testing with real high school players — Cooper is onboarding a few soon


================================================================================
[Prior session summaries below]
================================================================================

SESSION SUMMARY — June 2026 (session 4)
Cooper Teaches Tennis | CooperTeachesTennis/CTTAcademy
================================================================================

WHAT WE BUILT
--------------

1. ACTIVE / INACTIVE PLAYERS
   Players can now be marked Inactive instead of deleted. This preserves all
   session history, LTTDP, and profile data so Cooper can pick up where he left
   off when a player returns after a break.

   How it works:
   - `active: true` added to all player objects (old records default to true on read)
   - Coach sees "Mark Inactive" / "Reactivate" toggle button on player.html
   - Inactive badge appears next to the player name on player.html
   - Dashboard: inactive players greyed out with an "Inactive" badge; hidden by
     default — "Show Inactive" checkbox reveals them
   - Profile.html: if a player is inactive, a modal dialog appears once per
     browser session: "Your Player Profile is marked as Inactive. Contact your
     coach to resume lessons." → OK dismisses it, player navigates normally
   - Registration: if an email is linked to an inactive player, registration is
     blocked with: "You already have an account that's marked as Inactive.
     Contact your coach to resume." (distinct from the normal 409 for active emails)
   - select.html: inactive players in the parent selector show an Inactive badge

   Files changed: worker/index.js, js/player.js, js/dashboard.js, js/profile.js,
                  js/select.js, js/register.js, css/style.css, dashboard.html

   Design decisions deferred:
   - Parent/child status inheritance (one parent → kids inherit active status) is
     deferred until the parent account model gets a full redesign. For now, active/
     inactive is per-player and Cooper manages it individually.

2. GROUP SESSIONS
   Session form now supports group lessons. A group session creates one session
   record per player, all linked by a shared groupSessionId.

   Create flow:
   - "Group lesson" checkbox appears on the session form (create mode only)
   - Checking it loads all active players into a scrollable checkbox list
   - The current player (from URL param) is pre-checked
   - Shared notes field (shown to all players in the session)
   - Optional per-player "Additional notes" textarea appears for each checked player
   - Submit creates one session record per selected player (minimum 2)

   Edit flow (group session):
   - Shows "Group session · N players" badge instead of player multi-select
   - Shared notes field — saving propagates the update to all group members'
     session records automatically via groupMemberSessionIds
   - Per-player individual notes field for the specific player being edited

   Data model additions to session objects:
     isGroup: true
     groupSessionId: UUID (shared across all players in the session)
     groupSize: number
     groupMemberSessionIds: [sessionId, ...] (for sharedNotes propagation)
     sharedNotes: string (shown to all players)
     individualNotes: string (per-player, optional)

   Individual sessions are unchanged: still use `notes` field.

   Display: both player.html and profile.html show group sessions with a
   "Group · N players" badge, shared notes, and a "Personal notes" section
   below if individual notes exist.

   Files changed: worker/index.js, js/session.js, js/player.js, js/profile.js,
                  css/style.css

3. ANALYTICS PANEL
   Stats panel at the top of the coach dashboard above the player list.

   Shows:
   - Active Players count
   - Sessions Taught (unique sessions — group sessions count as 1 regardless of
     how many players are in them, deduplicated by groupSessionId)
   - Inactive count (only shown if > 0)
   - NTRP breakdown chips (e.g. "3.0: 4", "3.5: 6") in NTRP order

   Endpoint: GET /api/analytics (coach-only)
   Deduplication: fetches all session objects and counts individual sessions +
   unique groupSessionIds. For a small coaching roster this is an acceptable
   number of KV reads.

   Files changed: worker/index.js, js/dashboard.js, dashboard.html, css/style.css

4. CSV EXPORT
   "Export CSV" button on the dashboard (next to "+ Add player").

   Exports active players only. Fields: First Name, Last Name, Email, Phone,
   NTRP Level, Improvement Goals, Member Since. Filename is dated:
   ctt-players-YYYY-MM-DD.csv

   Pure vanilla JS — no libraries. Uses Blob + URL.createObjectURL.
   Intended use: drop into Mailchimp or similar for announcements, discounts,
   newsletters. Inactive players are excluded automatically (they don't receive
   bulk emails).

   Files changed: js/dashboard.js, dashboard.html


DESIGN DECISIONS MADE THIS SESSION
-------------------------------------
- Soft delete (inactive) instead of hard delete — preserves history for returning players
- Inactive dialog shows once per browser session (sessionStorage flag `ctt_inactive_ack`)
- Group session minimum is 2 players
- sharedNotes propagation: editing shared notes on any player's session record
  updates all other group members' records automatically
- Analytics deduplicates by groupSessionId so a 6-person group lesson counts as
  1 session, not 6
- CSV exports active players only — inactive players are automatically off the
  distribution list
- Parent/child active inheritance deferred to a future session

5. GROUP SESSION PLAYER MULTI-SELECT — FILTER + CHIPS
   Improved the group session player picker for large rosters.

   Changes:
   - Filter input (🔍) above the checkbox list — Cooper types a name and the
     list narrows in real time, no API calls, pure JS filtering
   - Selected players appear as green chips (e.g. "Sarah J. ×") above the
     filter input so Cooper can see the full group at a glance without scrolling
   - Clicking × on a chip deselects that player, unchecks their checkbox, and
     removes their individual notes field — everything stays in sync
   - List is still sorted alphabetically by last name (from the API)
   - No backend changes needed

   Files changed: js/session.js, css/style.css


6. USER_GUIDE.md — COACH USER GUIDE CREATED
   Plain-language guide written for Cooper covering every coach-facing function:
   logging in, the dashboard (analytics, search, inactive toggle, CSV export),
   adding players, editing profiles, marking inactive/reactivating, LTTDP,
   logging individual and group sessions, editing sessions, and a quick-reference
   table at the end.

   Convention established: USER_GUIDE.md should be updated at the end of every
   session when new coach-facing features are added. CLAUDE.md now includes this
   as a standing rule.

   File created: USER_GUIDE.md


OPEN / DEFERRED
-----------------
1. Parent/guardian account model redesign — one parent email → child sub-profiles
   that inherit active/inactive status from the parent. Needs its own session.
2. Confirm Cooper's real social media URLs → update linktree.html
3. Update About Me content in worker/index.js → handleGuestInfo() with Cooper's real bio
4. Custom domain setup (cooperteachestennis.com) via Cloudflare when ready
5. Instagram Reels on About Me page — discussed but deferred until Cooper decides
   he wants it. If yes: Worker fetches Instagram Graph API server-side; clean approach
   that avoids external scripts and CSP violations.
6. GitHub Pages on CooperTeachesTennis/CTTAcademy — requires Cooper to enable in
   repo Settings (org admin access — Mitch can't do it)


================================================================================
[Prior session summaries below]
================================================================================

SESSION SUMMARY — June 2026 (session 3)
Cooper Teaches Tennis | CooperTeachesTennis/CTTAcademy
================================================================================

WHAT WE FIXED / BUILT
-----------------------

1. COACH AUTH — SWITCHED FROM COOKIES TO SESSIONSTORAGE + HEADER
   Root cause: the Worker (ctt-worker.andermd535.workers.dev) and Pages
   (ctt-academy.pages.dev) are on different domains. Chrome and Safari block
   cross-site cookies even with SameSite=None, so the session cookie set by
   the Worker after OAuth was never sent back on subsequent requests — coach
   pages appeared to load then immediately redirect to the landing page.

   New flow:
   - After GitHub OAuth, the Worker redirects to:
       dashboard.html#session=TOKEN
     (URL fragment — never sent to any server, not in browser history)
   - auth.js captures the token from the hash on page load, stores it in
     sessionStorage as 'ctt_coach_token', and strips the hash from the URL
   - All coach API requests now send: X-Coach-Token: TOKEN (header)
   - Worker validates the X-Coach-Token header instead of a cookie
   - Logout sends X-Coach-Token to the Worker to delete the KV record,
     then clears sessionStorage

   Security tradeoff documented in CLAUDE.md: sessionStorage is readable by
   JS (unlike HttpOnly cookies), but the strict CSP mitigates XSS. This
   approach is CSRF-immune. Revisit when a custom domain is set up.

   Files changed: worker/index.js, js/auth.js, js/api.js

2. MULTI-COACH SUPPORT — OWNER_GITHUB_ID NOW COMMA-SEPARATED
   Previously only one GitHub user ID could be the coach. Updated the Worker
   to split OWNER_GITHUB_ID on commas and check against the full list.
   Cooper's numeric GitHub user ID was added to the secret:
     wrangler secret put OWNER_GITHUB_ID
     value: 283034047,COOPERS_ID
   Both Mitch and Cooper can now log in as coach independently.
   No redeploy needed — Worker secrets take effect immediately.

   Files changed: worker/index.js

3. SESSION.HTML — FIXED ENDLESS SPINNER
   session.js references document.getElementById('nav-back-link') to render
   a "← Player" back link, but the element didn't exist in session.html.
   The JS threw a TypeError silently, execution stopped, and the page was
   stuck showing the default spinner forever. This bug was pre-existing but
   only discoverable after auth was working (nobody could reach session.html
   before the auth fix above).
   Fixed by adding <div id="nav-back-link"> to session.html.

   Files changed: session.html


CURRENT STATE OF OWNER_GITHUB_ID SECRET
-----------------------------------------
  Both Mitch Anderson (283034047) and Cooper are set.
  To add or change IDs in the future:
    wrangler secret put OWNER_GITHUB_ID
    Enter: ID1,ID2   (comma-separated, no spaces required but trim-safe)
  To find a GitHub numeric user ID:
    https://api.github.com/users/USERNAME  → look for "id" field


================================================================================
SESSION SUMMARY — June 1, 2026
Cooper Teaches Tennis | CooperTeachesTennis/CTTAcademy
================================================================================

LATE-SESSION CHANGES (added after initial summary was written)
---------------------------------------------------------------
1. COACH NAV UNIFIED WITH PUBLIC NAV
   After OAuth, Cooper now sees the same tab nav as public users, but with
   "Your Players" as the second tab instead of "Player Profile". The tabs are:
   Home | Your Players | Store | Education | Link Tree | About Me
   This nav appears on dashboard.html, player.html, and session.html.
   Future: Cooper will eventually get editing privileges on Home, Store,
   Education, and About Me pages from within this same nav.

2. PLAYER PROFILE — STATIC VIEW WITH EDIT TOGGLE
   Player Info and LTTDP sections now display as read-only static text by
   default. Each section has its own "Edit" button. Clicking Edit reveals the
   form fields. Save returns to static view with updated data. Cancel discards
   changes. Player Info and LTTDP edit independently.

3. DASHBOARD — REMOVED DUPLICATE ADD PLAYER BUTTON
   The "+ Add player" link was removed from the nav bar. It only appears in
   the main body of the dashboard now.

4. PARENT/GUARDIAN EMAIL — FLAGGED FOR REDESIGN
   Removed parent email field from the coach add/edit player form. The field
   still exists in the data model and player self-registration form. The parent
   account model (one parent email → multiple child profiles) needs a proper
   redesign before it's ready for the coach-facing UI. Flagged for a future
   session.

5. COACH LOGIN — HIDDEN SECRET URL
   "Coach Login" button removed from the public landing page entirely.
   Cooper accesses the dashboard via a private bookmark:
     https://ctt-academy.pages.dev/coach
   This URL is not linked from anywhere on the site. coach.html is a simple
   redirect page that sends Cooper to GitHub OAuth on the Worker.
   Security: even if someone finds /coach, they cannot pass OAuth without
   Cooper's GitHub account. The Worker validates a specific numeric GitHub
   user ID server-side.

6. AUTH.JS FIXED — ABSOLUTE WORKER URL
   auth.js was using relative URLs for /api/auth/check and /api/auth/logout,
   which resolved to the Pages domain instead of the Worker. Fixed to use the
   full Worker URL: https://ctt-worker.andermd535.workers.dev

7. CORS FIXED IN WORKER
   Worker was using Access-Control-Allow-Origin: * which is incompatible with
   credentials: 'include'. Fixed to return the specific requesting origin
   (from an allowlist) with Access-Control-Allow-Credentials: true. Allowlist:
   https://ctt-academy.pages.dev and https://cooperteachestennis.github.io.
   Session cookie changed to SameSite=None to support cross-origin requests.

8. OAUTH REDIRECT FIXED
   After GitHub OAuth, the Worker was redirecting to its own origin instead of
   the Pages frontend. Fixed to redirect to https://ctt-academy.pages.dev/dashboard.html.

--------------------------------------------------------------------------------

WHAT WE BUILT
-------------
A full coaching management platform built from scratch in a single session.
Live at: https://ctt-academy.pages.dev

The site has two distinct sides:
  - A public-facing site (Home, Store, Education, Link Tree, About Me) visible
    to anyone, plus a Player Profile tab for registered players
  - A private coach dashboard only Cooper can access

All data and security logic runs through a Cloudflare Worker. The browser never
touches data directly — everything goes through the Worker, which validates auth
before doing anything.


HOW COOPER LOGS IN (READ THIS FIRST)
-------------------------------------
Cooper has a private bookmark: https://ctt-academy.pages.dev/coach

This URL is not linked or advertised anywhere on the site. Visiting it redirects
to GitHub OAuth, which verifies Cooper's identity. Once logged in, the session
lasts 7 days — so on most days the dashboard just opens directly from the
bookmark with no login step at all.

Security: even if someone finds the /coach URL, they can't get in unless they're
logged into Cooper's specific GitHub account. The Worker checks a numeric user ID
server-side — no other GitHub account can pass that check.

When the 7-day session expires: open the bookmark, tap "Authorize" on GitHub
(usually one tap, no password needed if already logged into GitHub on that
device), and the dashboard opens.

Mitch's GitHub account (MitchAndrsn, ID: 283034047) is currently set as the
owner for dev/testing. To transfer full coach control to Cooper later:
  1. Cooper creates a GitHub account
  2. Find his numeric user ID at: https://api.github.com/users/HIS_USERNAME
  3. Run: wrangler secret put OWNER_GITHUB_ID
  4. Enter Cooper's numeric ID when prompted
  Done — Cooper now has coach access, Mitch no longer does.


PLAYER FLOW
-----------
1. Player visits ctt-academy.pages.dev
2. Enters their email address → clicks "Access My Profile"
3. If found: goes straight to their profile page (read-only)
4. If multiple profiles found (parent with kids): sees a selector screen to pick
   which player to view
5. If not found: offered to create a profile
6. Registration requires: first name, last name, email, phone (all required —
   hard stop if any are missing). Optional: NTRP level, improvement goals,
   parent/guardian email
7. Parent email: if provided, that parent email also gets access to this player's
   profile. A parent with two registered kids sees both profiles on login.
8. Session data stored in browser sessionStorage — clears when tab closes
   (intentional — appropriate for shared devices)


WHAT PLAYERS SEE ON THEIR PROFILE
----------------------------------
- Their contact info and NTRP level
- Long-Term Tennis Development Plan (LTTDP) — four sections:
    Goals / Technical Skills / Patterns & Plays / On-Off Season
  Cooper fills these in from the dashboard. Players see them read-only.
- Full session history — every session Cooper has logged, newest first
  Each session shows: date, duration, topics covered, notes
  Group sessions show a "Group · N players" badge, shared notes, and
  personal notes below if Cooper added any for that player specifically.


WHAT COOPER SEES (COACH DASHBOARD)
------------------------------------
- Analytics panel: active player count, sessions taught, inactive count, NTRP breakdown
- List of all active players, sortable, with name search
- "Show Inactive" toggle to reveal greyed-out inactive players
- "Export CSV" button — downloads active players for email distribution
- "Add Player" button to register a player manually (same form as self-reg)
- Click any player → full edit view:
    - Edit profile info (name, email, phone, NTRP, goals)
    - Mark Inactive / Reactivate toggle
    - Edit all four LTTDP sections (Save LTTDP button)
    - Full session history with Edit button per session
    - "Log Session" button → session form
- Session log form:
    - Shows LAST SESSION at the top before Cooper writes new notes
    - "Group lesson" toggle → multi-player checkbox list
    - Shared notes (all players) + optional per-player individual notes
    - Works in both create and edit mode


INFRASTRUCTURE / ACCOUNTS
---------------------------
Cloudflare account:   Mitch Anderson (andermd535@gmail.com)
Worker URL:           https://ctt-worker.andermd535.workers.dev
Pages URL (live):     https://ctt-academy.pages.dev
GitHub repo:          https://github.com/CooperTeachesTennis/CTTAcademy
KV Namespace ID:      945aa45b37d94e22a71a5271a004b50c
KV Preview ID:        ddf7b4d41a964abc8995343066cec6a1

Worker secrets (set via `wrangler secret put`, never in any file):
  GITHUB_CLIENT_ID     — OAuth app client ID
  GITHUB_CLIENT_SECRET — OAuth app client secret (rotated during setup)
  OWNER_GITHUB_ID      — 283034047,COOPERS_ID (Mitch + Cooper)

GitHub OAuth app:
  Name:         Cooper Teaches Tennis
  Callback URL: https://ctt-worker.andermd535.workers.dev/api/auth/callback


HOW TO DEPLOY CHANGES
-----------------------
After editing any file:

  # Deploy Worker (backend changes):
  wrangler deploy

  # Deploy frontend (HTML/CSS/JS changes):
  wrangler pages deploy /Users/mitchanderson/CTTA --project-name ctt-academy --commit-dirty=true

  # Push to GitHub (keeps repo in sync):
  git add -A && git commit -m "your message" && git push origin main

All three are independent. Worker and Pages can be deployed separately.
